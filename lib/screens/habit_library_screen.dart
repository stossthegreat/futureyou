import 'package:flutter/material.dart';
import 'what_if_screen.dart';

/// Wrapper for the Habit Library (old What-If screen with preset habits)
class HabitLibraryScreen extends StatelessWidget {
  const HabitLibraryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const WhatIfScreen();
  }
}

